# Template only — copy to secret.yaml, fill in real values, and DO NOT commit
# secret.yaml (it's gitignored). `kubectl create secret` from a .env file is
# usually cleaner than hand-editing this, but this documents exactly which
# keys the Deployments expect.
apiVersion: v1
kind: Secret
metadata:
  name: hybrid-rag-docs-secrets
  namespace: hybrid-rag-docs
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"
  DATABASE_URL: "postgresql+psycopg://postgres:changeme@postgres:5432/hybrid_rag_docs"
  TEST_DATABASE_URL: "postgresql+psycopg://postgres:changeme@postgres:5432/hybrid_rag_docs_test"
  LLM_API_KEY: ""
  AUTH_SECRET: ""
  AUTH_GOOGLE_ID: ""
  AUTH_GOOGLE_SECRET: ""
